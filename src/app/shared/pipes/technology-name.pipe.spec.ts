import { TechnologyNamePipe } from "./technology-name.pipe";

describe('TechnologyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TechnologyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
